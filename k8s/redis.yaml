apiVersion: apps/v1
kind: Deployment
metadata:
  name: hms-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hms-redis
  template:
    metadata:
      labels:
        app: hms-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: hms-redis
spec:
  ports:
  - port: 6379
  selector:
    app: hms-redis
